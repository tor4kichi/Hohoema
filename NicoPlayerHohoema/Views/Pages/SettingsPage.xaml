<Page x:Class="NicoPlayerHohoema.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="using:NicoPlayerHohoema.Views"
      xmlns:mvvm="using:Prism.Mvvm"
      xmlns:view="using:NicoPlayerHohoema.Views"
      xmlns:i="using:Microsoft.Xaml.Interactivity"
      xmlns:core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:mybehavior="using:NicoPlayerHohoema.Views.Behaviors"
      xmlns:toolkit="using:WinRTXamlToolkit.Controls"
      xmlns:uwpcontrols="using:Microsoft.Toolkit.Uwp.UI.Controls"
      xmlns:iconPack="using:MahApps.Metro.IconPacks"
      xmlns:uwptoolkitControl="using:Microsoft.Toolkit.Uwp.UI.Controls"
      xmlns:hardTrigger="using:AdaptiveTriggerLibrary.Triggers.HardwareInterfaceTriggers"
      xmlns:myconverters="using:NicoPlayerHohoema.Views.Converters"
      xmlns:uwpui="using:Microsoft.Toolkit.Uwp.UI.Extensions"
      xmlns:templateselector="using:NicoPlayerHohoema.Views.TemplateSelector"
      xmlns:windowsStateTriggers="using:WindowsStateTriggers"
      DataContext="{x:Null}"
      mc:Ignorable="d"
      x:Name="PageRoot"
      >
  <Page.Resources>
    <DataTemplate x:Key="RemovableListItemTemplate">
      <Button Background="Transparent"
              UseSystemFocusVisuals="True"
              HorizontalAlignment="Stretch"
              HorizontalContentAlignment="Left"
              Padding="8 0 0 0">
        <Button.Flyout>
          <MenuFlyout>
            <MenuFlyoutItem Text="削除"
                            Command="{Binding RemoveCommand}" />
          </MenuFlyout>
        </Button.Flyout>
        <toolkit:DockPanel Background="Transparent"
                           Margin="16 0">
          <TextBlock Text="{Binding Label}"
                     VerticalAlignment="Center"></TextBlock>
        </toolkit:DockPanel>
      </Button>
    </DataTemplate>
    <DataTemplate x:Key="TextWithRemoveButtonTemplate">
      <toolkit:DockPanel>
        <TextBlock Text="{Binding Content}"
                   toolkit:DockPanel.Dock="Left"
                   VerticalAlignment="Center" />
        <AppBarButton Icon="Remove"
                      Command="{Binding RemoveCommand}" />
      </toolkit:DockPanel>
    </DataTemplate>
    <Style x:Key="SubTitleStyle"
           TargetType="TextBlock">
      <Setter Property="FontSize"
              Value="20" />
      <Setter Property="Margin"
              Value="0 0" />
    </Style>
    <Style x:Key="ContentTextStyle"
           TargetType="TextBlock">
      <Setter Property="Margin"
              Value="0 0 0 8" />
      <Setter Property="TextWrapping"
              Value="Wrap" />
    </Style>
    <Style TargetType="ToggleSwitch"
           BasedOn="{StaticResource ToggleSwitchNoHeader}">
      <Style.Setters>
        <Setter Property="MinWidth"
                Value="0" />
      </Style.Setters>
    </Style>
    <Style x:Key="SettingsGroupStackPanel"
           TargetType="StackPanel">
      <Setter Property="Spacing"
              Value="20" />
      <Setter Property="Margin"
              Value="0 0 0 16" />
    </Style>
    <Style x:Key="SettingsMiddleGroupStackPanelStyle"
           TargetType="StackPanel">
      <Setter Property="Spacing"
              Value="16" />
      <Setter Property="Margin"
              Value="16 0 0 0" />
    </Style>
    
    <Style x:Key="SettingsOptionTitleTextBlock"
           TargetType="TextBlock">
      <Setter Property="FontWeight"
              Value="SemiBold" />
      <Setter Property="VerticalAlignment"
              Value="Center" />
    </Style>
    <Style x:Key="SettingsSubHeaderHorizonBorderStyle"
           TargetType="Border">
      <Setter Property="BorderBrush"
              Value="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" />
      <Setter Property="BorderThickness"
              Value="0 1 0 0" />
      <Setter Property="VerticalAlignment"
              Value="Center" />
      <Setter Property="Margin"
              Value="8 0 0 0" />
    </Style>
  </Page.Resources>
  <Grid>
    <uwptoolkitControl:DockPanel>
      <StackPanel x:Name="HeaderPanel" uwptoolkitControl:DockPanel.Dock="Top" Background="{StaticResource MenuBackgroundBrush}">
        <CommandBar x:Name="VideoListCommandBar" Background="Transparent">
          <CommandBar.Content>
            <Border Background="Transparent" IsHitTestVisible="True" Margin="16 0 0 0"></Border>
          </CommandBar.Content>
          <CommandBar.PrimaryCommands>
            <AppBarButton Content="エラーログ" Command="{Binding ShowErrorFilesFolderCommand}" Visibility="{Binding IsDebugModeEnabled.Value, Converter={StaticResource BooleanToVisibility}}" />
            <AppBarButton Background="Transparent">
              <AppBarButton.Flyout>
                <MenuFlyout>
                  <MenuFlyoutItem Text="バージョン番号をコピー" Command="{Binding CopyVersionTextToClipboardCommand}" />
                  <ToggleMenuFlyoutItem Text="デバッグモードを有効にする" IsChecked="{Binding IsDebugModeEnabled.Value,Mode=TwoWay}" />
                </MenuFlyout>
              </AppBarButton.Flyout>
              <StackPanel Orientation="Horizontal">
                <TextBlock Opacity="0.5" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center">
                                        v<Run Text="{Binding VersionText}" /></TextBlock>
              </StackPanel>
            </AppBarButton>
          </CommandBar.PrimaryCommands>
        </CommandBar>
      </StackPanel>
      <ScrollViewer MaxWidth="480" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel x:Name="ContentPanel" Spacing="16">
          <!-- アプリの表示設定 -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="アプリの表示設定" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <StackPanel Style="{StaticResource SettingsMiddleGroupStackPanelStyle}">
              <Grid>
                <TextBlock Text="起動時の表示ページ" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                <ComboBox ItemsSource="{Binding StartupPageTypeList}" SelectedItem="{Binding StartupPageType.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right">
                  <ComboBox.ItemTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding Converter={StaticResource ToLocalText}}" />
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </Grid>
              <Grid>
                <StackPanel>
                  <TextBlock Text="テーマ" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                  <TextBlock Text="※テーマの適用にはアプリを再起動してください" Foreground="Red" Margin="0 8" Visibility="{Binding ThemeChanged, Converter={StaticResource BooleanToVisibility}}" />
                </StackPanel>
                <ComboBox ItemsSource="{Binding ThemeList}" SelectedItem="{Binding SelectedApplicationTheme.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" />
              </Grid>
              <Grid x:Name="DefaultFullScreenSwitchLayout" Visibility="Collapsed">
                <StackPanel>
                  <TextBlock Text="アプリ起動時にフルスクリーン表示を設定する" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                </StackPanel>
                <ToggleSwitch IsOn="{Binding IsDefaultFullScreen.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" />
              </Grid>
              <Grid x:Name="AppDisplayModeSettingLayout">
                <TextBlock Text="{Binding Source=ApplicationInteractionMode, Converter={StaticResource LocalizeConverter}, Mode=OneTime}" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                <ComboBox ItemsSource="{Binding ElementName=PageRoot, Path=OverrideInteractionModeList}" SelectedItem="{Binding AppearanceSettings.OverrideIntractionMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" IsSynchronizedWithCurrentItem="False">
                  <ComboBox.ItemTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding Converter={StaticResource LocalizeConverter}, FallbackValue={Binding Source=NoSelected, Converter={StaticResource LocalizeConverter}}}" />
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </Grid>
            </StackPanel>
          </StackPanel>
          <!-- NG設定 -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="NG設定" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <StackPanel Style="{StaticResource SettingsMiddleGroupStackPanelStyle}">
              <Grid>
                <TextBlock Text="動画投稿者IDによるフィルタを有効にする" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                <ToggleSwitch IsOn="{Binding NGVideoOwnerUserIdEnable.Value, Mode=TwoWay}" HorizontalAlignment="Right" />
              </Grid>
              <StackPanel Margin="0 8">
                <uwpcontrols:Expander Header="NG指定した動画投稿者を表示" HorizontalContentAlignment="Left" Foreground="DimGray">
                  <ListView ItemsSource="{Binding NGVideoOwnerUserIds}" Margin="16 8" IsTabStop="False" HorizontalContentAlignment="Stretch" uwpui:ListViewBase.Command="{Binding OpenUserPageCommand}" IsItemClickEnabled="True" SelectionMode="None">
                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Border Background="Transparent" IsHitTestVisible="True" BorderBrush="{ThemeResource ButtonBorderThemeBrush}" BorderThickness="1" Padding="16 8">
                          <TextBlock Text="{Binding Description}" VerticalAlignment="Center" />
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ListView.ItemsPanel>
                      <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                      </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                  </ListView>
                </uwpcontrols:Expander>
              </StackPanel>
              <Grid>
                <TextBlock Text="動画タイトルによるフィルタを有効にする" toolkit:DockPanel.Dock="Left" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                <ToggleSwitch IsOn="{Binding NGVideoTitleKeywordEnable.Value, Mode=TwoWay}" HorizontalAlignment="Right" />
              </Grid>
              <StackPanel Visibility="{Binding NGVideoTitleKeywordEnable.Value, Converter={StaticResource BooleanToVisibility}}">
                <TextBox Text="{Binding NGVideoTitleKeywords.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" PlaceholderText="NG動画タイトルを入力" ScrollViewer.VerticalScrollBarVisibility="Auto" Height="120" />
                <TextBlock Text="{Binding NGVideoTitleKeywordError.Value}" Visibility="{Binding NGVideoTitleKeywordError.Value, Converter={StaticResource NotNullToVisibility}}" />
                <TextBlock Margin="0 8" Foreground="Gray">
                                    ・１行ごとにフィルタ文字列を指定します
                                    ・正規表現が利用できます</TextBlock>
              </StackPanel>
            </StackPanel>
          </StackPanel>
          <!-- キャッシュ設定 -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="キャッシュ設定" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <StackPanel Style="{StaticResource SettingsMiddleGroupStackPanelStyle}">
              <Grid Margin="0 8">
                <TextBlock Text="デフォルトのキャッシュ画質" Style="{StaticResource SettingsOptionTitleTextBlock}" />
                <ComboBox ItemsSource="{Binding AvairableCacheQualities}" SelectedItem="{Binding DefaultCacheQuality.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right">
                  <ComboBox.ItemTemplate>
                    <DataTemplate>
                      <TextBlock>
                                                <Run Text="{Binding Converter={StaticResource ToLocalText}}" /></TextBlock>
                    </DataTemplate>
                  </ComboBox.ItemTemplate>
                </ComboBox>
              </Grid>
              <!--


                            <StackPanel Margin="0 8">
                                <TextBlock Text="従量課金ネットワーク利用時の動画キャッシュダウンロードを許可する"
                                   toolkit:DockPanel.Dock="Left"
                                   VerticalAlignment="Center"
                                           TextWrapping="Wrap"
                                           MaxWidth="240"
                                           HorizontalAlignment="Left"
                                   />
                                <ToggleSwitch 
                                  IsOn="{Binding IsAllowDownloadOnMeteredNetwork.Value, Mode=TwoWay}"
                                  OnContent="許可"
                                    OffContent="不許可"
                                          />
                            </StackPanel>


                            <StackPanel Margin="0 8">
                                <TextBlock Text="従量課金ネットワーク利用時のキャッシュ画質"
                           VerticalAlignment="Center"
                           />
                                <ComboBox ItemsSource="{Binding AvairableDefaultCacheQualitiesOnMeteredNetwork}" 
                                          SelectedItem="{Binding DefaultCacheQualityOnMeteredNetwork.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          >
                                    <ComboBox.ItemTemplateSelector>
                                        <templateselector:NicoVideoCacheQualityTemplateSelector>
                                            <templateselector:NicoVideoCacheQualityTemplateSelector.UnknownTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="「デフォルトのキャッシュ画質」と同じ" />
                                                </DataTemplate>
                                            </templateselector:NicoVideoCacheQualityTemplateSelector.UnknownTemplate>
                                            <templateselector:NicoVideoCacheQualityTemplateSelector.DefaultTemplate>
                                                <DataTemplate>
                                                    <TextBlock>
                                                        <Run Text="{Binding Converter={StaticResource ToLocalText}}" />
                                                    </TextBlock>
                                                </DataTemplate>
                                            </templateselector:NicoVideoCacheQualityTemplateSelector.DefaultTemplate>
                                            
                                        </templateselector:NicoVideoCacheQualityTemplateSelector>
                                        
                                    </ComboBox.ItemTemplateSelector>
                                </ComboBox>
                            
                            
                            </StackPanel>
                            
                            -->
            </StackPanel>
          </StackPanel>
          <!-- このアプリについて -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="このアプリについて" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <StackPanel Style="{StaticResource SettingsMiddleGroupStackPanelStyle}">
              <Grid>
                <TextBlock Text="アプリのバージョン" Opacity="0.7" VerticalAlignment="Center" />
                <TextBlock HorizontalAlignment="Right">
                                v<Run Text="{Binding VersionText}" /></TextBlock>
              </Grid>
              <Button Content="更新情報を見る" Command="{Binding ShowUpdateNoticeCommand}" CommandParameter="{Binding CurrentVersion}" HorizontalAlignment="Right" />
              <HyperlinkButton Content="Hohoemaのプロジェクトページ" Margin="0 0 0 8" NavigateUri="https://github.com/tor4kichi/Hohoema" UseSystemFocusVisuals="True" HorizontalAlignment="Right" />
              <HyperlinkButton Content="Hohoemaのプライバシーポリシー" Margin="0 0 0 8" NavigateUri="https://github.com/tor4kichi/Hohoema/wiki/Privacy-policy" UseSystemFocusVisuals="True" HorizontalAlignment="Right" />
            </StackPanel>
          </StackPanel>
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="フィードバック" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <HyperlinkButton Content="匿名質問サービス＜マシュマロ＞からメッセージを送る" Margin="0 0 0 8" NavigateUri="https://marshmallow-qa.com/tor4kichi" UseSystemFocusVisuals="True" HorizontalAlignment="Right" />

            <HyperlinkButton Content="ストアでHohoemaをレビューする" Margin="0 0 0 8" Command="{Binding LaunchAppReviewCommand}" UseSystemFocusVisuals="True" HorizontalAlignment="Right" />
          </StackPanel>
          <!--  -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="利用ライブラリの一覧" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <ItemsControl ItemsSource="{Binding LisenceItems}" Margin="0 8 8 8" IsTabStop="False">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <HyperlinkButton Margin="0 0" NavigateUri="{Binding Site}" Content="{Binding Name}" HorizontalAlignment="Right" />
                  <!--
                                    <StackPanel Orientation="Horizontal"
                                        Margin="8 8 0 0"                                            
                                        >
                                        <TextBlock
                                        >
                                    <Run Text="Lisence:" Foreground="Gray"  />
                                    <Run Text="{Binding LisenceType}"
                                        >
                                
                                    </Run>
                                        </TextBlock>


                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="16 8 0 0">

                                        <TextBlock Text="Author:" Foreground="Gray"  />

                                        <ItemsControl ItemsSource="{Binding Authors}"
                                                    IsTabStop="False"
                                                    >

                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Vertical">

                                                    </StackPanel>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"
                                                    Margin="8 0 0 1"
                                                    />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>


                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal"
                                        Margin="8 8 0 0"                                            
                                        >
                                        <TextBlock>
                                    <Run Text="Project Site:" Foreground="Gray" />
                                    <Hyperlink NavigateUri="{Binding Site}">
                                        <Run Text="{Binding Site}" />                                                                            
                                    </Hyperlink>
                                        </TextBlock>
                                    </StackPanel>


                                    <Button Content="ライセンスの全文を表示"
                                        Margin="8 16 8 16"
                                        UseSystemFocusVisuals="True"
                                        >
                                        <Button.ContextFlyout>
                                            <Flyout Placement="Full">
                                                <StackPanel>

                                                    <TextBlock Margin="0 32" HorizontalAlignment="Center" TextWrapping="Wrap">
                                                <Run Text="原文"></Run>
                                                <Hyperlink NavigateUri="{Binding LisencePageUrl}"
                                                            >
                                                    <Run Text="{Binding LisencePageUrl}" />
                                                </Hyperlink>
                                                    </TextBlock>




                                                    <Border Height="8" BorderBrush="Black" BorderThickness="0 1 0 0" />

                                                    <TextBlock Text="{Binding LisenceText}" MaxWidth="400" TextWrapping="Wrap" />

                                                </StackPanel>
                                            </Flyout>
                                        </Button.ContextFlyout>
                                    </Button>


                                    <Border
                                Margin="0 8"
                                BorderBrush="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" BorderThickness="0 1 0 0" />
                                    -->
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
          <!-- その他 -->
          <StackPanel>
            <toolkit:DockPanel Margin="0 8">
              <TextBlock Text="その他" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" toolkit:DockPanel.Dock="Left" />
              <Border Style="{StaticResource SettingsSubHeaderHorizonBorderStyle}" />
            </toolkit:DockPanel>
            <ListView ItemsSource="{Binding PurchaseItems}" uwpui:ListViewBase.Command="{Binding ShowCheerPurchaseCommand}" IsItemClickEnabled="True" SelectionMode="None">
              <ListView.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <toolkit:DockPanel>
                      <Image Source="{Binding ProductListing.ImageUri}" Height="48" Width="48" toolkit:DockPanel.Dock="Left" />
                      <StackPanel Orientation="Horizontal" toolkit:DockPanel.Dock="Bottom" Margin="0 0 0 8">
                        <TextBlock Margin="8 0" VerticalAlignment="Center">
                                                <Run Text="{Binding ProductListing.FormattedPrice}" /></TextBlock>
                        <Border Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibility}}" Margin="8 0">
                          <TextBlock Text="(購入済み)" FontWeight="SemiBold" VerticalAlignment="Center" />
                        </Border>
                      </StackPanel>
                      <TextBlock Text="{Binding ProductListing.Name}" Margin="8 0" VerticalAlignment="Center" />
                    </toolkit:DockPanel>
                  </Grid>
                </DataTemplate>
              </ListView.ItemTemplate>
            </ListView>
          </StackPanel>
        </StackPanel>
      </ScrollViewer>
    </uwptoolkitControl:DockPanel>
    <VisualStateManager.VisualStateGroups>
      <!-- レイアウトモード -->
      <VisualStateGroup>
        <VisualState>
          <VisualState.StateTriggers>
            <windowsStateTriggers:EqualsStateTrigger Value="{Binding ApplicationLayoutManager.AppLayout}" EqualTo="TV" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="HeaderPanel.Margin" Value="{StaticResource ContentListViewHeaderMargin_TV}" />
            <Setter Target="ContentPanel.Margin" Value="{StaticResource ContentListViewBodyMargin_TV}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <windowsStateTriggers:EqualsStateTrigger Value="{Binding ApplicationLayoutManager.AppLayout}" EqualTo="Desktop" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="HeaderPanel.Margin" Value="{StaticResource ContentListViewHeaderMargin_Desktop}" />
            <Setter Target="ContentPanel.Margin" Value="{StaticResource ContentListViewBodyMargin_Desktop}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <windowsStateTriggers:EqualsStateTrigger Value="{Binding ApplicationLayoutManager.AppLayout}" EqualTo="Tablet" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="HeaderPanel.Margin" Value="{StaticResource ContentListViewHeaderMargin_Tablet}" />
            <Setter Target="ContentPanel.Margin" Value="{StaticResource ContentListViewBodyMargin_Tablet}" />
          </VisualState.Setters>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <windowsStateTriggers:EqualsStateTrigger Value="{Binding ApplicationLayoutManager.AppLayout}" EqualTo="Mobile" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="HeaderPanel.Margin" Value="{StaticResource ContentListViewHeaderMargin_Mobile}" />
            <Setter Target="ContentPanel.Margin" Value="{StaticResource ContentListViewBodyMargin_Mobile}" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
  </Grid>
</Page>
