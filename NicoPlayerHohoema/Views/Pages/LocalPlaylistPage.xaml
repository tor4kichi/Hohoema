<Page
    x:Class="NicoPlayerHohoema.Views.LocalPlaylistPage"
  x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:videolistPage="using:NicoPlayerHohoema.Views.Pages.VideoListPage"
  xmlns:mytrigger="using:NicoPlayerHohoema.Views.StateTrigger"
  xmlns:i="using:Microsoft.Xaml.Interactivity"
  xmlns:mybehavior="using:NicoPlayerHohoema.Views.Behaviors"
  xmlns:core="using:Microsoft.Xaml.Interactions.Core"
  xmlns:flyouts="using:NicoPlayerHohoema.Views.Flyouts"
  xmlns:controls="using:NicoPlayerHohoema.Views.Controls"
  xmlns:windowsTriggers="using:WindowsStateTriggers"
  mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
  NavigationCacheMode="Enabled"
  >

  <Grid>

    <videolistPage:VideoItemsListView ItemsSource="{Binding PlaylistItems}"
                                      ItemCommand="{Binding HohoemaPlaylist.PlayCommand}"
                                      RefreshCommand="{Binding RefreshCommand}"
                                      PlaylistPassToFlyout="{Binding Playlist}"
                                      x:Name="VideoItemsListView"
                                      >
      <videolistPage:VideoItemsListView.ItemContextFlyoutTemplate>
        <DataTemplate>
          <flyouts:VideoItemFlyout />
        </DataTemplate>
      </videolistPage:VideoItemsListView.ItemContextFlyoutTemplate>

      <videolistPage:VideoItemsListView.Header>
        <controls:ScrollHeader x:Name="ScrollHeader"
                        Mode="QuickReturn" >

          <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <CommandBar x:Name="VideoListCommandBar" Background="Transparent" OverflowButtonVisibility="Auto" >

              <CommandBar.Content>
                <StackPanel x:Name="TitleLayout" Margin="16 0 0 0">
                  
                </StackPanel>
              </CommandBar.Content>

              <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="RemoveWatchedItemsButton"
                  Label="{Binding Source=RemoveWatchedItems, Converter={StaticResource LocalizeConverter}}"
                              Command="{Binding RemoveWatchedItemsInAfterWatchPlaylistCommand}"
                              Icon="Delete"
                  />
                <!-- 非選択中 -->
                <AppBarButton x:Name="PlayAllButton" Icon="Play"
                              Label="{Binding Source=PlayAll, Converter={StaticResource LocalizeConverter}, Mode=OneTime}" 
                              Command="{Binding PlaylistPlayAllCommand}"
                              CommandParameter="{Binding Playlist}"
                              >
                </AppBarButton>
                <AppBarButton x:Name="ScrollToTopButton" Icon="Up" Label="{Binding Source=ReturnToPageTop, Converter={StaticResource LocalizeConverter}, Mode=OneTime}">
                  <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="Click">
                      <core:CallMethodAction TargetObject="{x:Bind VideoItemsListView}" MethodName="ResetScrollPosition" />
                    </core:EventTriggerBehavior>
                  </i:Interaction.Behaviors>
                </AppBarButton>
              </CommandBar.PrimaryCommands>

              <CommandBar.SecondaryCommands>
                
                <AppBarButton x:Name="DeletePlaylistButton"
                              Command="{Binding LocalPlaylistDeleteCommand}"
                              CommandParameter="{Binding Playlist}"
                              Background="Transparent"
                              Visibility="Collapsed"
                              Icon="Delete"
                              Label="{Binding Source=LocalPlaylistDelete, Converter={StaticResource LocalizeConverter}, Mode=OneTime}"
                              >
                </AppBarButton>
              </CommandBar.SecondaryCommands>
            </CommandBar>
          </StackPanel>
        </controls:ScrollHeader>

      </videolistPage:VideoItemsListView.Header>

      <videolistPage:VideoItemsListView.ItemTemplate>
        <DataTemplate>
          <Grid>
            <videolistPage:VideoListItemControl />
          </Grid>
        </DataTemplate>
      </videolistPage:VideoItemsListView.ItemTemplate>

    </videolistPage:VideoItemsListView>

    <Grid x:Name="EmptyItemsLayout" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
      <TextBlock Text="{Binding Source=LocalPlaylistNoItems, Converter={StaticResource LocalizeConverter}, Mode=OneTime}"
                     Style="{StaticResource SubtitleTextBlockStyle}"
                     Opacity="0.8"
                     />
    </Grid>


    <VisualStateManager.VisualStateGroups>

      <!-- トップバー表示時のタイトル位置ずらし -->
      <VisualStateGroup>
        <VisualState>
          <VisualState.StateTriggers>
            <StateTrigger IsActive="{StaticResource IsTVModeEnabled}" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="VideoItemsListView.Padding" Value="48 0 48 27" />
            <Setter Target="ScrollHeader.Mode" Value="None" />
          </VisualState.Setters>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <AdaptiveTrigger MinWindowWidth="{StaticResource WidthBreakPoint_WideToMiddle}" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
          </VisualState.Setters>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <AdaptiveTrigger MinWindowWidth="0" />
          </VisualState.StateTriggers>
          <VisualState.Setters>

          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup>
        <VisualState>
          <VisualState.StateTriggers>
            <StateTrigger IsActive="{Binding IsWatchAfterPlaylist}" />
          </VisualState.StateTriggers>
        </VisualState>
      </VisualStateGroup>
      <VisualStateGroup>

        <VisualState>
          <VisualState.StateTriggers>
            <StateTrigger IsActive="{Binding IsWatchAfterPlaylist}" />
            <StateTrigger IsActive="{Binding IsQueuePlaylist}" />
          </VisualState.StateTriggers>
        </VisualState>
        <VisualState>
          <VisualState.StateTriggers>
            <StateTrigger IsActive="True" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="DeletePlaylistButton.Visibility" Value="Visible" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>


      <VisualStateGroup>
        <VisualState>
          <VisualState.StateTriggers>
            <windowsTriggers:EqualsStateTrigger Value="{Binding PlaylistItems.Count}" EqualTo="0" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="EmptyItemsLayout.Visibility" Value="Visible" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>

    </VisualStateManager.VisualStateGroups>

  </Grid>
</Page>
