<MenuFlyout
    x:Class="NicoPlayerHohoema.Views.Controls.LiveListItemFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <MenuFlyoutItem Text="小窓で再生"
                                Command="{Binding PlayWithSmallPlayerCommand}"
                                />

    <MenuFlyoutItem Text="コミュニティ情報を開く" 
                    Command="{Binding OpenCommunityPageCommand}"
                    />

    <MenuFlyoutSeparator />

    <MenuFlyoutItem Text="共有" Command="{Binding ShareCommand}" Visibility="{Binding IsXbox, Converter={StaticResource BooleanToVisibility}}" />

    <MenuFlyoutSubItem Text="共有" Visibility="{Binding IsXbox, Converter={StaticResource BooleanToVisibilityInverted}}" >
        <MenuFlyoutItem Text="アプリを選択" Command="{Binding ShareCommand}" />
        <MenuFlyoutItem Text="Twitterで共有" Command="{Binding ShereWithTwitterCommand}" />
        <MenuFlyoutItem Text="コピー" Command="{Binding VideoInfoCopyToClipboardCommand}" />
    </MenuFlyoutSubItem>

</MenuFlyout>
