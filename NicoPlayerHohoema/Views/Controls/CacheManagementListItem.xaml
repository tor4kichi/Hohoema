<UserControl
    x:Class="NicoPlayerHohoema.Views.Controls.CacheManagementListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views.Controls"
    xmlns:view="using:NicoPlayerHohoema.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:toolkit="using:WinRTXamlToolkit.Controls"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:mybehavior="using:NicoPlayerHohoema.Views.Behaviors"
    xmlns:ext="using:WinRTXamlToolkit.Controls.Extensions"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <toolkit:DockPanel Margin="8">
        
        <Border toolkit:DockPanel.Dock="Bottom" >
            <toolkit:DockPanel Visibility="{Binding IsVisibleProgress.Value, Converter={StaticResource BooleanToVisibility}}">

                <ProgressBar Maximum="100.0" 
                             Minimum="0.0" 
                             Value="{Binding ProgressPercent.Value}"
                             Margin="0 4"
                             />
            </toolkit:DockPanel>
        </Border>

        <Border >


            <toolkit:DockPanel>

                <!-- 状態 -->
                <StackPanel  toolkit:DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding CacheRequestTime}" Margin="0 0 0 4" />
                    <TextBlock Text="{Binding Quality}" Margin="0 0 0 4" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CacheState.Value}" />
                        <TextBlock toolkit:DockPanel.Dock="Left" Margin="8 0"
                                                Visibility="{Binding IsVisibleProgress.Value, Converter={StaticResource BooleanToVisibility}}"
                                                >
                            <Run Text="(" />
                            <Run Text="{Binding ProgressPercent.Value}" />
                            <Run Text="%)" />
                        </TextBlock>
                    </StackPanel>

                </StackPanel>

                <!-- 操作 -->

                <view:VideoInfoControl />

            </toolkit:DockPanel>

        </Border>

    </toolkit:DockPanel>
</UserControl>
