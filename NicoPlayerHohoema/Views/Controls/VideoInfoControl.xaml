<UserControl
    x:Class="NicoPlayerHohoema.Views.VideoInfoControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="using:NicoPlayerHohoema.Views"
    xmlns:toolkit="using:WinRTXamlToolkit.Controls"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:behavior="using:NicoPlayerHohoema.Views.Behaviors"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="400">

    <Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{Binding IsForceDisplayNGVideo}" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NGVideoShutoutWall.Visibility" Value="Collapsed" />
                        <Setter Target="VideoInfo.Visibility" Value="Visible" />
                        <Setter Target="VideoInfo.IsHitTestVisible" Value="True" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{Binding IsNotGoodVideo}" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NGVideoShutoutWall.Visibility" Value="Visible" />
                        <Setter Target="VideoInfo.Visibility" Value="Collapsed" />
                        <Setter Target="VideoInfo.IsHitTestVisible" Value="False" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        
        
        
        <toolkit:DockPanel x:Name="VideoInfo" IsHitTestVisible="True" Background="Transparent">

            <StackPanel Orientation="Horizontal" toolkit:DockPanel.Dock="Left" Width="8"
                        ToolTipService.ToolTip="青=通常画質保存済み / 緑=低画質保存済み / 赤=削除確認が必要"
                        >
                <Border BorderThickness="4 0 0 0" BorderBrush="Blue" 
                        Visibility="{Binding IsOriginalQualityCached.Value, Converter={StaticResource BooleanToVisibility}}"
                        />
                <Border BorderThickness="2 0 2 0" BorderBrush="Green" 
                        Width="6"
                        Visibility="{Binding IsLowQualityCached.Value, Converter={StaticResource BooleanToVisibility}}"
                        />

            </StackPanel>

            <Grid Height="60" Width="80" toolkit:DockPanel.Dock="Left">

               
                <Image  Source="{Binding ThumbnailImageUrl}"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"
                    Stretch="Fill"
                   ></Image>

                <Border Background="#AA000000"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="4"
                    >
                    <TextBlock Text="{Binding MovieLength, Converter={StaticResource TimeToMovieLengthConverter}}"
                       Foreground="White"
                           FontSize="12"
                           Margin="2"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                       />
                </Border>

                <!-- NGVideoのときだけ表示するボタン -->
                <Button Content="NG"
                        Command="{Binding StopNGVideoDisplayCommand}" 
                        Visibility="{Binding IsNotGoodVideo, Converter={StaticResource BooleanToVisibility}}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                    />

            </Grid>


            <toolkit:DockPanel Margin="8 0 0 0" >

                <!-- post at -->
                <Border toolkit:DockPanel.Dock="Top" Margin="0 0 0 0">
                    <TextBlock Foreground="Gray" FontSize="12">
                        <Run Text="{Binding PostAt}" />
                        <Run Text="投稿" />
                    </TextBlock>
                </Border>

                

               

                <StackPanel Orientation="Horizontal" toolkit:DockPanel.Dock="Bottom">
                    <TextBlock Margin="0 0 8 0" Foreground="Gray" FontSize="12">
                        <Run Text="再生" />
                        <Run Text="{Binding ViewCount, Converter={StaticResource ToUserFriendlyNumber}}" />
                    </TextBlock>
                    <TextBlock Margin="0 0 8 0" Foreground="Gray" FontSize="12">
                        <Run Text="コメ" />
                        <Run Text="{Binding CommentCount, Converter={StaticResource ToUserFriendlyNumber}}" />
                    </TextBlock>
                    <TextBlock Margin="0 0 8 0" Foreground="Gray" FontSize="12">
                        <Run Text="マイ" />
                        <Run Text="{Binding MylistCount, Converter={StaticResource ToUserFriendlyNumber}}" />
                    </TextBlock>
                </StackPanel>


                <TextBlock Text="{Binding Title}" 
                           FontSize="16"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                       />
                
            </toolkit:DockPanel>
        </toolkit:DockPanel>


        <Grid Visibility="{Binding IsDeleted, Converter={StaticResource BooleanToVisibility}}"
              Background="#60ffffff"
              >
            <SymbolIcon Symbol="Delete"
                        HorizontalAlignment="Left"
                        Margin="32 0 0 0"
                        Width="48"
                        Height="48"
                        />

        </Grid>


        <Grid x:Name="NGVideoShutoutWall"
            Background="#01000000"
              Visibility="Collapsed"
              >

            <Button Content="NGを一時的に解除"
                        Command="{Binding ForceDisplayNGVideoCommand}" 
                        HorizontalAlignment="Left"
                        VerticalAlignment="Stretch"
                        Foreground="Gray"                        
                        Margin="16 0 0 0"
                    />
            
            <toolkit:DockPanel HorizontalAlignment="Center"
                               VerticalAlignment="Center">
                <TextBlock Text="NG"
                           FontSize="32"
                           toolkit:DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Foreground="Gray"
                           Margin="16 0"
                           />

                
                
                <TextBlock HorizontalAlignment="Center"
                           Foreground="Gray"
                           VerticalAlignment="Center"
                           >
                    <Run Text="{Binding NGVideoReason}" />
                </TextBlock>

                

            </toolkit:DockPanel>
        </Grid>


    </Grid>

    
</UserControl>
