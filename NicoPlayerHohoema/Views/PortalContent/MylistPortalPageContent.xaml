<UserControl
    x:Class="NicoPlayerHohoema.Views.PortalContent.MylistPortalPageContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views.PortalContent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:toolkit="using:WinRTXamlToolkit.Controls"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="MylistGroupListItemTemplate">
            <toolkit:DockPanel>

                <Border Background="Transparent" IsHitTestVisible="True">

                    <i:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="Tapped">
                            <core:InvokeCommandAction Command="{Binding OpenMylistCommand}" />
                        </core:EventTriggerBehavior>
                    </i:Interaction.Behaviors>

                    <TextBlock Text="{Binding Title}"
                               VerticalAlignment="Center"
                               />

                </Border>
            </toolkit:DockPanel>
        </DataTemplate>
        
    </UserControl.Resources>

    <toolkit:DockPanel>
        <StackPanel toolkit:DockPanel.Dock="Top">

            <Border Background="Transparent" IsHitTestVisible="True">
                <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="Tapped">
                        <core:InvokeCommandAction Command="{Binding OpenMylistCommand}" />
                    </core:EventTriggerBehavior>
                </i:Interaction.Behaviors>

                <TextBlock Text="マイリスト" />


            </Border>

            <Border Height="32" />

        </StackPanel>

        <ScrollViewer HorizontalScrollMode="Disabled"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollMode="Auto"
                      VerticalScrollBarVisibility="Hidden"
                      >

            <ListView ItemsSource="{Binding MylistGroupItems}"
                  ItemTemplate="{StaticResource MylistGroupListItemTemplate}"
                      Margin="0 0 0 16"
                  >
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>

            </ListView>
        </ScrollViewer>
    </toolkit:DockPanel>
</UserControl>
