<UserControl
    x:Class="NicoPlayerHohoema.Views.PortalContent.FavPortalPageContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views.PortalContent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:toolkit="using:WinRTXamlToolkit.Controls"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    mvvm:ViewModelLocator.AutoWireViewModel="True"    
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <DataTemplate x:Key="FavoriteItemTemplate">
            <Border Background="Transparent">
                <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="Tapped">
                        <core:InvokeCommandAction Command="{Binding SelectedCommand}" />
                    </core:EventTriggerBehavior>

                </i:Interaction.Behaviors>

                <TextBlock Text="{Binding Title}" />

            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <StackPanel>
        <Border>
            <TextBlock Text="お気に入り" >
                <i:Interaction.Behaviors>
                    <core:EventTriggerBehavior EventName="Tapped">
                        <core:InvokeCommandAction Command="{Binding OpenFavFeedListCommand}" />
                    </core:EventTriggerBehavior>
                </i:Interaction.Behaviors>

            </TextBlock>
        </Border>

        <Border Height="32" />


        <ItemsControl ItemsSource="{Binding Lists}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0 4">
                        <TextBlock Text="{Binding Name}" 
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Right"
                                   Foreground="Gray"
                                   FontSize="20"
                                           />

                        <ListView ItemsSource="{Binding Items}"
                                          ItemTemplate="{StaticResource FavoriteItemTemplate}"
                                          >
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <toolkit:WrapPanel Orientation="Vertical"
                                                               />

                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>

                        </ListView>

                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
