<Page
    x:Class="NicoPlayerHohoema.Views.RankingCategoryListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NicoPlayerHohoema.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:view="using:NicoPlayerHohoema.Views"
    xmlns:toolkit="using:WinRTXamlToolkit.Controls"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mvvm:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer HorizontalScrollBarVisibility="Hidden">
            <StackPanel Orientation="Vertical">
                
                <ListView ItemsSource="{Binding HighPriorityRankingCategoryItems}"
                          SelectedItem="{Binding SelectedRankingCategory.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectionMode="Single"
                          DisplayMemberPath="Label"
                          >

                </ListView>

                <Border BorderThickness="1 0 0 0" BorderBrush="Gray" />

                <ListView ItemsSource="{Binding MiddlePriorityRankingCategoryItems}"
                          SelectedItem="{Binding SelectedRankingCategory.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectionMode="Single"
                          DisplayMemberPath="Label"
                          >

                </ListView>

                <Border BorderThickness="1 0 0 0" BorderBrush="Gray" />

                <ListView ItemsSource="{Binding LowPriorityRankingCategoryItems}"
                          SelectedItem="{Binding SelectedRankingCategory.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          SelectionMode="Single"
                          DisplayMemberPath="Label"
                          >
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Foreground" Value="Gray" />
                        </Style>
                    </ListView.ItemContainerStyle>

                </ListView>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
