<UserControl
    x:Class="Hohoema.Presentation.Views.Controls.BooleanViewSwitcher"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hohoema.Presentation.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:ts="using:Hohoema.Presentation.Views.TemplateSelector"
  mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <Grid>
    <ContentControl x:Name="DefaultContentControl"
                    ContentTemplate="{x:Bind DefaultViewTemplate, Mode=OneWay}"
                    Content="{Binding}"
                    VerticalContentAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsTabStop="False"
                    Visibility="Visible"
                    >
      
    </ContentControl>

    <ContentControl x:Name="SwitchedContentControl"
                    ContentTemplate="{x:Bind SwitchedViewTemplate, Mode=OneWay}"
                    Content="{Binding}"
                    VerticalContentAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    IsTabStop="False"
                    Visibility="Collapsed"
                    >

    </ContentControl>
    
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup>
        <VisualState>
          <VisualState.StateTriggers>
            <StateTrigger IsActive="{x:Bind IsSwitchView, Mode=OneWay}" />
          </VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="DefaultContentControl.Visibility" Value="Collapsed" />
            <Setter Target="SwitchedContentControl.Visibility" Value="Visible" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
  </Grid>
</UserControl>
